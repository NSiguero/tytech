# Use exact versions to avoid unnecessary updates
save-exact=true

# Use pnpm store efficiently
store-dir=./.pnpm-store

# Prefer pnpm over npm
prefer-pnpm=true

# Use the latest pnpm version
engine-strict=true
